Drop full texts or excerpts here (plain text / markdown) for influences the
model wouldn't already know. `tools/distill_style.ts` reads every file in this
folder (first ~20k chars each) alongside `../canon.md`. This README is skipped.
