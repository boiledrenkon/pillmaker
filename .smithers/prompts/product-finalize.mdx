Finalize this concept's outputs on disk, matching the existing repo convention,
then return the structured result.

### Values (already filled in — use these verbatim; MDX props, no placeholders)

- OUTDIR: {props.outDir}
- SLUG: {props.slug}
- STATUS: {props.status}
- ACCEPTED image path (empty/blank = none): {props.acceptedPath}
- ALL candidate paths produced this run (newline-separated):

{props.candidates}

### Convention (mirror the existing OUTDIR/SLUG folders)

- Accepted image is copied to OUTDIR/SLUG/SLUG.png
- Final prompt is written to OUTDIR/SLUG/SLUG.txt
- Rejected candidates are moved into OUTDIR/SLUG/fail/ (keep their filenames)
- Reference images used: leave any provided refs where they are; if the spec's
  refs were copied into the concept folder, keep them under
  OUTDIR/SLUG/ref_images/.

### Steps

1. Create the OUTDIR/SLUG/ folder and its fail/ subfolder.
2. If STATUS is accepted and an ACCEPTED image path is given above: copy that
   file to OUTDIR/SLUG/SLUG.png and write the final prompt (below) to
   OUTDIR/SLUG/SLUG.txt.
3. Move every *other* candidate (and, if STATUS is rejected, all of them) into
   fail/.
4. Return status, finalImage (the saved .png path or null), promptPath (the
   saved .txt path or null), and fails (list of paths moved to fail/).

Use cp / mkdir -p / mv via your shell tool. Do not delete anything outside the
concept's own folder.

### Final prompt

{props.prompt}
